
项目总结：
   有buffer和blockqueue2个辅助类
   通过main初始化ip，port，sql等，首先初始化webserver，日志，初始化线程池（可能要问模板下为什么不能分为src和h文件实现）和连接池，
都采用blockqueue来存储task和sqlcon，连接池通过信号量来实现来获取sqlcon等于0则阻塞等待连接对象，（若过用while会空转，cv要配合锁），
为了保证对sqlcon对象的回收，没有采用RAII（资源获取即初始化），而是采用shared-ptr的reset，自定义删除器，在智能指针析构前把sqlcon
送去blockqueue，并让post信号量。
  随后设置listenfd和clientfd为ET还是LT，对于clinet还要设置EPOLLONESHOT，防止一个连接被多个线程同时处理，若为ET，还要设置他们分别为
非阻塞，因为在ET模式下epoll只会通知一次，要把所有数据都read/write，或者accept，否则每次只有一部分被处理，while处理则当处理完成，
在非nonblocking状态下会阻塞，在此状态下才会返回 EAGAIN，表示处理完毕。
  初始化listenfd，加入epoll，并设置2个状态，
   1、SO_REUSEADDR让服务器在关闭后，立即重启，不会因为client处于time-wait状态而失败（必须在 bind() 之前设置）
      不会消除time-wait状态
    2、SO_LINGER（逗留），调用 close() 时如何处理未发送数据，
    linger option{
    .l_onoff = 1,
    .l_linger = 0
};（00）：代表默认，正常返回close，内核继续处理数据和发送FIN
   （1，1）：close最多阻塞一秒，可提前返回
   （1，0）：强制关闭，不发送剩余数据于FIN,发送RST
    3、lusten（，1024），代表全连接队列
  启动服务器，等待连接，如果开启计时器，则把epoll的等待时间设置为下一次tick的时间，同时处理超时连接和待处理连接，否则一直阻塞，直到有可处理连接。
1、listen连接则添加fd，并加入计时器，可读和可写处理时回延长计时器的时间。
2、可读则将处理加入线程池队列，通过httpcon的read读取，调用httpcon的process函数，调用httprequst解析报文。
通过状态机处理请求行（方法/路径/版本），请求头（长来凝结之类），请求体（post才），在解析会结合正则表达处理（可以用string优化），
如果有请求体，会判断是否为URL编码，是则解码。=和&在作为分割符是不会编码，保持不变，其他的数字变为%16位进制的形式，将16位进制的进行转换成对应的char
，随后通过sql连接验证登录密码等（看一下细节）

，将解析的参数传给response对象，调用makerepsonse构造响应报文。
随后通过process的返回值，判断是否处理成功，成功则注册为EPOLLOUT，因为EPOLLONESHOT要每次重新添加到epoll中，否为EPOLLIN。
3、可写，也是将函数加入线程池队列，线程会将处理好的响应报文和资源，通过writev写入fd。
随后通过返回值和erron，判断是否要关闭连接，如果写缓存区的剩余字节为0，且表示长连接，则关闭连接，否则设置为epollin
如果不为0，且erron时EAGAIN，此时代表内核缓冲区已满，则设置为EPOLLOUT。
否则就是出现问题，关闭连接。

关闭服务器时，将listen关闭，sqlcon关闭，将线程池的关闭标志置为true，线程池的线程都时detach（），会自动回收。
detach在主线程结束后会自动回收，但是可能访问以及析构对象，用join更好

为什么必须join？
等待线程结束、建立同步关系、回收线程资源，并保证相关对象在线程使用期间不会提前销毁。
如果不join，会直接std::terminate，任务被终止

//进行加密和处理sql注入

mysql_res 存储mysql——quary的结果集
mysql——quary是查询的api（sql对象，string）
res=mysql——store——result 获取结果集
mysql——free-result 释放结果集（不然会内存泄露）
MYSQL_ROW row=mysql_fetch_row(res)取出第一行数据，row就是一个数据，多大取决于select了几个
while可不断取出

准备进行大修，加入主从Reactor和兼容mysql和lsm，并加入sql预处理和加盐hash
将线程池分为：
CPU pool（为了加大破解成本，会减慢解密速度，因此会阻塞，将其于http其他的分开）
    密码哈希等 CPU 密集任务

Storage pool：
    MySQL / TinyLSM 访问

实现htto增量 Parser，完善整个协议解析

memory map mmap(内存映射)：
把文件的一段内容映射到进程的虚拟地址空间。映射成功后，可以像访问普通内存一样访问文件内容
void* mmap(
    void* addr,//通常不指定映射起始地址，os选择
    size_t length,//映射多少字节
    int prot,
    int flags,
    MAP_PRIVATE：映射允许写入，修改也不会写回原文件，通常采用写时复制
    MAP_SHARED：修改映射区域可能写回原文件，其他映射该文件的进程也可能看到修改
    int fd,
    off_t offset//一般必须是系统页面大小的整数倍
);
成功返回成功时返回映射区域的起始地址
MAP_FAILED
对应的文件并未一定在内存，只是建立映射，且由内核维护，fd可关闭，
只能用munmap关闭



主从reacto
one loop per thread 代表一个线程一个事件循环
为了让线程只处理自己的loop，每个loop都有任务队列：
用于把“必须在某个 Reactor 线程中执行的操作”，安全地投递到该线程。

eventloop和eventloopthread实现了，pool还没有
在webserver中实现分发函数，在绑定到acceptor的callback（用来分发到对应的任务队列）
还要有一个函数来处理add，用lambda包装一层，不用写函数，用addfd来从当即可

处理到分发函数，细节还要处理一下，wake已做，还要一个函数做writewake和readwake

fd有几种类型，因此内部的数据结构不一样，socket的SOCK_STRAM/SOCK_DGRAM
分别是tcp（字节流），udp（数据报）
tcp不存在报文，没有报文边界，read没有字节流会阻塞，返回0代表关闭
udp数据报协议，可以发送长度为 0 的 UDP datagram

event的内部是一个64位计数器
eventfd（wakefd），因此对他的读写要uint64，8字节一次

eventfd(0, EFD_NONBLOCK | EFD_CLOEXEC);
eventfd——t value
eventfd_read、eventfd_write
只是对read和write的封装

blockdeque的内部有了cv和mutex，因此在sqlpool中其实不用semid和锁

云盘：
文件表：
user_id,
file_id（自动增长）,
file_name（逻辑名字）,
object——id
CREATE TABLE file (
    file_id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    file_name VARCHAR(255) NOT NULL,UNIQUE KEY,
    object_id BIGINT UNSIGNED NOT NULL,

    KEY idx_user_id(user_id),
    KEY idx_object_id(object_id)
);

分享表:
分享时，返回share_token，通过share_token来确定是哪个文件，再通过code_hash
null则是公开的，否则需要提取码来验证，成功后redis增加一个暂时的cookie(sha256_hex(token)->share_id，)
file-id可能有多个分享状态，因此要用share_id，
在短时内不需要再次验证提取码，最后下载时验证并获取file-id，再下载


CREATE TABLE share (
    share_id        BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    share_token     CHAR(32) NOT NULL UNIQUE,//通过这个确定文件
    file_id         BIGINT UNSIGNED NOT NULL,
    //如果用object-id，那么删除分享时不能区分删除哪个
    code_hash       VARCHAR(8) NULL,//null代表公开，非null代表需要提取码，暂定4位

    expire_time     DATETIME NULL,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,

    KEY idx_file_id (file_id)
);

用户表：
user_id(这个通过mysql的自动增长实现),
username,
pwd（哈希加密后的）

物理存储表
CREATE TABLE object (
    object_id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,

    content_hash CHAR(64) NOT NULL,

    file_size BIGINT UNSIGNED NOT NULL,

    storage_path VARCHAR(512) NOT NULL,

    ref_count BIGINT UNSIGNED NOT NULL DEFAULT 1,

    UNIQUE KEY uk_content_hash(content_hash)
);

通过cookies来区分用户，不能明文传输user-id，不然无法区分攻击报文，先登录然后返回cookies，然后存储cookies和user-id的对应
每次的http报文都含有cookies，在服务器进行user-id的查询，session层在webserver，通过单例模式让其他的调用，登录/注册的响应报文要加上这个

session：
对登录/注册的响应报文新增函数，在函数里通过session获取cookies
采用redis+mysql，因此它也是一个redis连接池
登录时要重新验证，退出时删除，受保护请求如上传，下载，分享要验证并延长时间（空闲30分钟即失效）
response直接持有cookies，因为在resie出错时path和code也要更改，因此要在构造响应报文之前进行插入
httpcoon的versityToken直接调用session的versityToken，在makeResponse进行gettokens

文件存储：


4个线程池:普通io，Auth_io，file_io,数据可io
subreactor用http处理后，根据状态调用函数，http下辖auth类，upload，download类
httprequst和httpcoon状态增加upload，download，conn的sta让subreactor知道该如何选择函数
auth：reactor调用auth-io和sql-io，handleAuth
upload：调用file-io，sql-io，handleUpload
download：调用file-io，sql-io，handeldowanload
httpconn:下辖upload，suth

普通的bode解析以及登录的操作依旧是之前的逻辑

上传现在httprequst的process中进行解析，增加了file知道将其解析到文件数据，然后返回upload，reactor调用函数
实则如auth一般，requst拥有fileupload类，上传的函数是回调的这里。

dlownload，解析按普通逻辑，报文按file makeresponse流式传输

对上传报文的非数据部分的解析已经完成，明天完成文件的传输（落盘以及元数据的存储，还有cookies，应该是根据cookies来判断用户）


buffer:
对vector<char>的封装，每次可写的大小为size-wpos,由于readv是线性读，只会一直向后，因此需要iov[2]
每次读时都将iov[0]的大小设置为size-wpos，另一片为临时缓存区，根据返回的len，小于size-wpos则将wpos向后移动len，
大于size-wpos则apeend（len），先判断是否大于可写，大于则扩充为刚好放下len的大小，小于直接加入。
由于在解析报文时可能没有解析完，因此wpos和rpos都不会变为0，因此存在空间浪费的问题，
retive当pos相等时置为0可以缓解这个问题，当报文解析完成必然重新开始

httpconn：
readv，采用buffer和缓冲区
writev，才让buffer+mmap文件
因此buffer的write没有用，可以用来作为upload的write

upload:
    初始化file——fd，evp-md-ctx*,每次通过writfd进行写，错误直接返回错误响应报文，写完后
通过evp-md-ctx*调用evp-digestupdata-ctx进行增量sha256，错误则evp-md-ctx-free释放资源，当文件上传完毕
会通过fsnyc落盘，随后通过evp-digestfina-ctx获取增量hash的结果，创建/objext/ab/cd/hash-hex的目录
在获取file-fd，通过这个进行renameat，普通的reaname会覆盖文件，这个只会返回，随后在落盘file-fd对目录项的改变
新文件：则新增object  重复文件：增加object的引用数量
采用on duplicate KEY updata 当key为唯一索引则冲突就执行后面的updata
这样就不用处理A,B同时上传一个文件是对数据库的同步

2个问题：一时验证redis时什么时候，第一次报文到达验证还是每一次都验证二时et模式下如果没有新报文会直接卡住，因此要一直读。知道缓冲区没了且
还没完时才needred

redis：
一是验证cookies，每个htttp请求只需要一次，在初始化file的filed时进行刚好
二是获取cookies，通过getrandom获取256bit，然后通过base64换成16进制的字符（6位一个，即3个字节4个字符）

download：
先打开文件，根据结果判断响应报文时download还是downloaderror，然后开始循坏发送file
由于采用sendfile，因此writebuf被放弃，全程不经过onwrite，直接在handle-down处理

对于epollin的fd，当client断开连接，会有eof信号，可读并关闭
epollout同理返回epollhup/epollrr

auth应该也放在process，验证和quary放在一起，这样webser就只调用conn的函数
conn也可以控制响应报文的构造，HttpConn::makeResponse根据参数传入 response_.MakeResponse(writeBuff_)
response_.MakeResponse增加一个参数HttpRequest::ParseResult  storage_path
这样addline等也可以增加HttpRequest::ParseResult，以此来控制报文的书写

ET下如果没有epollshot，在download进行轮换时，会一直沉睡，由于epollshot新注册会发出信号
此时可以继续写，这里的流量控制，体现在不会一直写，而是轮换，而大小的话，由于socket缓冲区时系统设置的
因此是固定的大小。进行EPOLL_CTL_MOD时，状态会重置为inactive，而 MOD 会重新检查当前 readiness
此时实际为可写/读，因此通知

share：  
   三种情况
   一、获取token，code（选择），会提交filename，是否要code，expiretime（由于没有采用stmt，因此不支持自定义时间）
   二、验证提取码，提交token，code（选择）（这里会先请求页面，根据是否又code，返回对应的html）
   三、sha256-hex（token），通过这个在redis中获取share-id，再进行验证是否是要下载的